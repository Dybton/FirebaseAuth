import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const ReadingBooksComponent = ({user}) => {
    return (
        <View>
            <Text>ReadingBooksComponent</Text>
        </View>
    )
}

export default ReadingBooksComponent

const styles = StyleSheet.create({})
